// initialise
var AccountComponents = (AccountComponents || {});

// transcription master controller
AccountComponents.projectCompletionBar = function (id, args) {

	// components
	return UI.createComponent(id, args).then(function (base) {

		// properties
		

		// methods

		return Promise.all([

		]).then(function () {
			return base
		});
	});

}