
// initialise
var AccountComponents = (AccountComponents || {});

// faq
AccountComponents.faq = function (id) {
	return UI.createComponent(id, {
		name: 'faq',
	}).then(function (base) {
		return Promise.all([
			
		]).then(function () {
			return base;
		});
	});
}